import { CardThumbnailDirective } from './card-thumbnail.directive';

describe('CardThumbnailDirective', () => {
  it('should create an instance', () => {
    const directive = new CardThumbnailDirective();
    expect(directive).toBeTruthy();
  });
});
