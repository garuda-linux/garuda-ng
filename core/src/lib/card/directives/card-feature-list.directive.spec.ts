import { CardFeatureListDirective } from './card-feature-list.directive';

describe('CardFeatureListDirective', () => {
  it('should create an instance', () => {
    const directive = new CardFeatureListDirective();
    expect(directive).toBeTruthy();
  });
});
